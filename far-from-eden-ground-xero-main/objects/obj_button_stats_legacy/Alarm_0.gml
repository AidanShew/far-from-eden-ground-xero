// Inherit the parent event
event_inherited();

room_goto(rm_menu); //This makes it to where when the button is pressed, the game will start.
//Putting goto in alarm 0 instead of left pressed allows the player to view the animation of the button